Generate a personalized growth plan for this learner.

Session summary:
```json
{{ session_json }}
```

Profile summary:
```json
{{ profile_json }}
```

Skill profile:
```json
{{ skill_profile_json }}
```

Preferences:
```json
{{ preferences_json }}
```

Allowed theme skill IDs in priority order:
```json
{{ allowed_theme_skill_ids_json }}
```

Planning hints and starter task patterns:
```json
{{ theme_templates_json }}
```

Available resources grouped by skill:
```json
{{ resources_by_skill_json }}
```

Build a plan that helps the learner make visible progress toward the target role using the available evidence and resources.
