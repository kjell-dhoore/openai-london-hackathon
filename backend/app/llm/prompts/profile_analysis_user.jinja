Analyze this learner and generate the structured profile output.

Learner session:
```json
{{ session_json }}
```

Profile intake:
```json
{{ intake_json }}
```

Quiz responses:
```json
{{ quiz_responses_json }}
```

Enabled sources:
```json
{{ enabled_sources_json }}
```

Candidate evidence documents:
```json
{{ evidence_documents_json }}
```

Skill library:
```json
{{ skill_library_json }}
```

Target-role skill guidance:
```json
{{ target_role_skill_map_json }}
```

Focus on what this learner is already good at, what most limits progress toward the target role, and which of the available internal sources best justify those conclusions.
